<%= form_with(model: staff_raw_product, local: true) do |form| %>
  <% if staff_raw_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(staff_raw_product.errors.count, "error") %> prohibited this staff_raw_product from being saved:</h2>

      <ul>
      <% staff_raw_product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :staff_id %>
    <%= form.text_field :staff_id %>
  </div>

  <div class="field">
    <%= form.label :raw_product_id %>
    <%= form.text_field :raw_product_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
