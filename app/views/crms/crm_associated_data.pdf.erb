<%=
  if params[:pdf_associated_data] == 'pdf_with_follow_ups'
    render 'crm_follow_ups'
  elsif params[:pdf_associated_data] == 'pdf_with_notes'
    render 'crm_notes'
  else
    render 'with_follow_ups_notes'
  end
%>