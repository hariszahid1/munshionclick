$('.show-data').children().remove();
<% if pos_setting_sys_type=="HousingScheme"%>
  $('.show-data').append("<%= escape_javascript(render partial: 'product_categories/hscheme/hscheme_new_form', locals: { product_category: @product_category }) %>");
<% else%>
  $('.show-data').append("<%= escape_javascript(render partial: 'new_form', locals: { product_category: @product_category }) %>");
<% end%>
$("#showdData").modal('show');