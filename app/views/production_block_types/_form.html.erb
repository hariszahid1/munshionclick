<%= bootstrap_form_for(production_block_type) do |f| %>
  <% if production_block_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(production_block_type.errors.count, "error") %> prohibited this production_block_type from being saved:</h2>

      <ul>
      <% production_block_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="col-lg-12 form-group float-left">
    <%= f.label :title do %>
          <%= ProductionBlockType.human_attribute_name :title %>
          <i class="fas fa-language"></i> 
        <% end %>
    <%= f.text_field :title,required:true,placeholder: "Enter the title", hide_label: true, class: 'urdu-font' %>
  </div>

  <div class="col-lg-12 form-group float-left">
    <%= f.number_field :quantity %>
  </div>

  <div class="col-lg-12 form-group float-left">
    <%= f.text_area :comment , placeholder:"Enter Comment"%>
  </div>

<div class="clearfix">
</div>

<div class="modal-footer">
    <button type="button" class="btn btn-danger" data-dismiss="modal">Close</button>
    <%=f.object.id.nil? ? (f.submit class: "btn btn-success mr-4", style: "float: right")  :  (f.submit class: "btn btn-warning mr-4", style: "float: right")%>
  </div>
          
            
          <% end %>
</div>
