<html>
  <head>
    <style>
      .border-bottom{
        border-bottom: double 4px;
      }
      .text-center{
        text-align: center;
      }
    </style>
  </head>
  <body>
    <% @date = Date.today  %>
    <% @time = DateTime.now %>
    <% @time = Time.zone.now %>
    <div>
      <p class="border-bottom"></p>
      <h1 class="text-center">User Types List</h1>
      <h1 class="text-center">
        Date: <b><span><%= @time.strftime("%d")%>/<%=@time.strftime("%b")%>/<%= @time.strftime("%y")%></span>
        Time: <span><%=@time.strftime("at %I:%M%p")%></span></b>
      </h1>
      <p class="border-bottom"></p>
      <p class="text-center">Report by: <%=current_user.name%></p>
    </div>
    <div>
      <table>
        <thead class="lite-gray">
          <th class="text-center">ID</th>
          <th class="text-center">Title</th>
          <th class="text-center">Code</th>
          <th class="text-center">Discount by percentage</th>
          <th class="text-center">Discount by amount</th>
          <th class="text-center">Comment</th>
        </thead>
        <tbody>
          <% data.each_with_index do |ut, index| %>
            <tr>
              <th class="text-center"><%= ut['id'] %></th>
              <td class="text-center"><%= ut['title'] %></td>
              <td class="text-center"><%= ut['code'] %></td>
              <td class="text-center"><%= ut['Discount_by_percentage'] %></td>
              <td class="text-center"><%= ut['Discount_by_amount'] %></td>
              <td class="text-center"><%= ut['comment'] %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </body>
</html>
