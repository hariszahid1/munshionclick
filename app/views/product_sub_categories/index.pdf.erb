<html>
  <head>
    <style>
      .border-bottom{
        border-bottom: double 4px;
      }
      .text-center{
        text-align: center;
      }
    </style>
  </head>
  <body>
    <% @date = Date.today  %>
    <% @time = DateTime.now %>
    <% @time = Time.zone.now %>
    <div>
      <p class="border-bottom"></p>
      <h1 class="text-center">Product Sub Categories List</h1>
      <h1 class="text-center">
        Date: <b><span><%= @time.strftime("%d")%>/<%=@time.strftime("%b")%>/<%= @time.strftime("%y")%></span>
        Time: <span><%=@time.strftime("at %I:%M%p")%></span></b>
      </h1>
      <p class="border-bottom"></p>
      <p class="text-center">Report by: <%=current_user.name%></p>
    </div>
    <div>
      <table>
        <thead class="lite-gray">
          <th>Category</th>
          <th>Title</th>
          <th>Code</th>
          <th>Comment</th>
        </thead>
        <tbody>
          <% data.each_with_index do |product_sub_category, index| %>
            <tr>
              <td><%= product_sub_category[:product_category].title %></td>
              <td><%= product_sub_category['title'] %></td>
              <td><%= product_sub_category['code'] %></td>
              <td><%= product_sub_category['comment'] %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  </body>
</html>
